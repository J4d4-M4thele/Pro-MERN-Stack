type Issue {
    id: Int!
    title: String!
    status: String!
    owner: String
    effort: Int
    created: String!
    due: String
}

##### Top level declarations

type Query {
    about: String!
    issueList: [issue!]!
}

type Mutation{
    setAboutMessage(message: String!): String
}